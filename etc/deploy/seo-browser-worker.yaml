apiVersion: apps/v1
kind: Deployment
metadata:
  name: sitelytics-seo-browser-worker
  namespace: utils
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: sitelytics-seo-browser-worker
  template:
    metadata:
      labels:
        app: sitelytics-seo-browser-worker
    spec:
      automountServiceAccountToken: false
      containers:
        - name: worker
          image: registry.tonisives.com/tgs/sitelytics-seo-browser:seo-20260925-7@sha256:76877a3846dd71adec34ffed87c6fce5cce21852e95c6839af4411512ec4e1ac
          envFrom:
            - secretRef:
                name: sitelytics-secrets
          env:
            - name: KAFKA_BROKERS
              value: redpanda.redpanda.svc.cluster.local:9093
            - name: SITELYTICS_URL
              value: http://sitelytics.utils.svc.cluster.local:19000
          resources:
            requests: {cpu: 100m, memory: 128Mi}
            limits: {cpu: "1", memory: 512Mi}
      imagePullSecrets:
        - name: tonisives-registry-secret
